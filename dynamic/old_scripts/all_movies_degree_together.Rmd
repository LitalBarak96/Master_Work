---
title: "time_sires_corr"
output: html_document
date: '2022-06-13'
---


This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r }

list_of_all_groups<-list.dirs(path = "D:/allGroups/Females_Grouped", full.names = TRUE, recursive = FALSE)

list_of_all_groups_degree_csv<-paste0(list_of_all_groups,"/","all_frames_degree.csv")
  full_df_of_csv<-NULL

library(dplyr)
for(i in 1:length(list_of_all_groups_degree_csv)){
  temp<-data.frame()
  temp<-as.data.frame(read.csv(list_of_all_groups_degree_csv[i]))
  full_df_of_csv<-bind_cols(full_df_of_csv,temp["Value"])
  colnames(full_df_of_csv)[ncol(full_df_of_csv)]<-basename(dirname(list_of_all_groups_degree_csv[i]))
  }

  
  full_df_of_csv<-cbind(time=1:2700,full_df_of_csv)
  
#library(ggplot2)
  

  
 # df <- melt(full_df_of_csv ,  id.vars = 'time', variable.name = 'series')

  
#  ggplot(full_df_of_csv, aes(x=time)) + geom_line()


```

